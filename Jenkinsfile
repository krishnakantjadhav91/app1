pipeline {
  agent any
  stages {
    stage("Initialize") {
      steps {
        echo "Testing"
      }

      stage("Build") {
        echo "Builing the application"
      }

      stage("Deploy") {
        echo "Deploy the Application"
      }      
    }
  }
}